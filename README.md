Install Dependencies

Bash
pip install opencv-python deepface numpy tf-keras

Create a folder named emotions containing these 7 PNG files:
happy.png, sad.png, angry.png, surprise.png, neutral.png, fear.png, disgust.png.

Run the Script

Bash
python main.py

Press q on your keyboard to quit the program.
